<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:CameraAndLocation"
             x:Class="CameraAndLocation.MainPage">
  <ContentPage.Content>
    <StackLayout>
      <Button Command="{Binding TakePictureCommand}" Text="Take picture" />
      <Image Source="{Binding ImageSource}" VerticalOptions="CenterAndExpand" />
      <Label Text="Status:" />
      <Label Text="{Binding Status}" />
      <Label Text="Current GPS Info:" />
      <Label Text="{Binding GPSText}" />
    </StackLayout>
  </ContentPage.Content>

</ContentPage>